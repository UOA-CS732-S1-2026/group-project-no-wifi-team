## Notes / Todo
 
- Data that needs to be stored (to be discussed on Thursday)

**Event Library:**
- JSON format: `{ key: { value: , cal: } }` — key represents different types, value represents the event
- Different events increase or decrease different attribute values

**Database:**
- Users (username & password)
- Time — in-progress time + achievement unlock time
- Attribute values
- Ending library + achievement library (badges awarded during play) + event library
- Not logged in — stored in browser (save file)